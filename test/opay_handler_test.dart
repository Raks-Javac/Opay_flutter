import 'package:flutter_test/flutter_test.dart';

//extends mockito and create a fake instance and initialize in your main function then use when to await the future and make use of the result
void main() {
  testWidgets('opay handler ...', (tester) async {});
}
